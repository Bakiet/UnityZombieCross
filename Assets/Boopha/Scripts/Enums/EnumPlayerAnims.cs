using UnityEngine;
using System.Collections;


/// <summary>
/// Enum player anims.
/// This enum is very importante because we use this numbers to control animation of our character
/// including triggers of animations.
/// </summary>
public enum EnumPlayerAnims : int 
{
	Idle = 0,
	Run = 1,
	Eat = 2,
	Win =3,
	Lose =4
}
