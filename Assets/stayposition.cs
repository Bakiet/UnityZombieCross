using UnityEngine;
using System.Collections;

public class stayposition : MonoBehaviour {

	public GameObject objectjtostay;
	public float xstay=0;
	public float ystay=0;
	public float zstay=0;
	// Use this for initialization
	void Start () {
		/*Vector3 v = objectjtostay.transform.position;
		v.x = objectjtostay.transform.position.x + xstay;
		v.y = objectjtostay.transform.position.y + ystay;
		v.z = objectjtostay.transform.position.z;
		transform.position = v;*/
	}
	
	// Update is called once per frame
	void Update () {

		/*Vector3 v = objectjtostay.transform.position;
		v.x = objectjtostay.transform.position.x + xstay;
		v.y = objectjtostay.transform.position.y + ystay;
		v.z = objectjtostay.transform.position.z;
		xstay = v.x;
		ystay = v.y;
		transform.position = v;
		*/
	
	}
}
